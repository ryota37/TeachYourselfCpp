Q. 次のプログラムがエラーになる理由を説明してください

int main()
{
    double d = 0.0;
    float f { d }; // エラー
}

myA.このコードではdouble型の変数をfloat型に変換しているため、縮小変換となっている。
ブレース({})を用いた変換では縮小変換は禁止されているため、エラーが出る

A. {}を使った初期化では、精度が悪くなる暗黙の方変換(縮小変換)が禁止されているためです。